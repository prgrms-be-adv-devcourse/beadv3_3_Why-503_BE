apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: npm-pvc
  namespace: flowin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi