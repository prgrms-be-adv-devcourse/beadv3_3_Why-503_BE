apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: flowin
spec:
  selector:
    app: reservation-service
  ports:
    - port: 8400       # [수정] Nginx가 8000번으로 들어옴
      targetPort: 8400 # [수정] 컨테이너의 8000번으로 연결
  type: ClusterIP