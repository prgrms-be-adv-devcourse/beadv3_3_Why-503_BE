name: Account Service CD (Test)

on:
  push:
    branches:
      - FI-222_feat_cd-sc-2
    paths:
      - 'FLOWIN/account-service/**'

jobs:
  releasing:
    uses: ./.github/workflows/cd-release-script.yml
    secrets: inherit
    permissions:
      contents: write
      attestations: write
      packages: write
    with:
      service-name: account-service